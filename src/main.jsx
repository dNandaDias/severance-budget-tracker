import React from 'react';
import ReactDOM from 'react-dom/client';
import SeveranceBudgetTracker from './SeveranceBudgetTracker.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SeveranceBudgetTracker />
  </React.StrictMode>
);
